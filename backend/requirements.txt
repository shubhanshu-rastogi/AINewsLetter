# ---- Web framework ----
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0

# ---- Settings & validation ----
pydantic[email]>=2.9,<3.0
pydantic-settings>=2.6,<3.0

# ---- Database ----
SQLAlchemy[asyncio]>=2.0,<3.0
asyncpg>=0.30,<1.0
alembic>=1.14,<2.0

# ---- Logging ----
structlog>=24.4

# ---- Workflow engine (foundation only; no graph logic yet) ----
langgraph>=0.2

# ---- LLM SDKs (installed for the foundation; not used yet) ----
openai>=1.55
anthropic>=0.40

# ---- HTTP client + collection ----
httpx>=0.27
feedparser>=6.0
beautifulsoup4>=4.12
APScheduler>=3.10,<4.0

# ---- Visual generation ----
Pillow>=10.3

# ---- Observability + ops ----
prometheus-client>=0.20
redis>=5.0

# ---- Secret encryption (UI-managed config at rest) ----
cryptography>=43.0

# ---- Optional: OpenTelemetry tracing (enable with ENABLE_TRACING=true) ----
# opentelemetry-sdk>=1.27
# opentelemetry-instrumentation-fastapi>=0.48b0
# opentelemetry-exporter-otlp-proto-http>=1.27
